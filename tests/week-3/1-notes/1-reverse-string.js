// reverse a string
const singleWord = "hello";
{
    for (let index = singleWord.length-1; index>=0; index--) {
        // const element = array[index];
        console.log(singleWord.charAt(index));
        
    }
}

